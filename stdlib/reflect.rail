# Define "string?"
[ type "string" == ] "string?" def

# Define its friends
[ "i64" "f64" "operator" "quotation" "bool" ] [
    dup
    [ type == ] swap push [ swap ] doin
    swap
    [ "?" ] swap push rev "" join
    def
] each
