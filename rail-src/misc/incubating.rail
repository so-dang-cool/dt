# TODO: This is not stdlib stuff. Move to another library

[ exec [ drop ] doin pop swap drop ] "exec-stdout" def

[ [ dup rot doin swap ] swap times drop ] "timesin" def

[ rev [ swap quote swap def ] each ] "->" def
